import React from 'react';

import './Toolbar.css';
import Logo from '../../Logo/Logo';

const toolbar = (props) => (
   <header className="Toolbar">
   <Logo/>
   <nav>John Doe</nav>
   </header>
);


export default toolbar;